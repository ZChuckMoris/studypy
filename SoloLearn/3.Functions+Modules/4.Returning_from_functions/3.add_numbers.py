def add_numbers(x, y):
  total = x + y
  return total
  print("This won't be printed")

# Line below does nothing. But if we use "print" on this function, we will get out answer (9)
add_numbers(4, 5)

print(add_numbers(4, 5))
